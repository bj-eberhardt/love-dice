services:
  web:
    image: node:24-alpine
    working_dir: /app
    command: sh -c "npm ci && npm run dev"
    ports:
      - "5544:5544"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
volumes:
  node_modules:
